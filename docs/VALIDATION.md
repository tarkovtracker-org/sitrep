# VALIDATION

## Decisions
- Scope: local Windows L81 trial, not a public production release or game-compatibility claim. SDK 10.0.303, WPF win-x64; Core/tests remain portable. No new package dependencies. Official checkout 7.0.1/setup-dotnet 6.0.0 are SHA-pinned (Node 24), replacing the CI-observed deprecated Node 20 action runtimes.
- Tesseract 5.2.0; official English fast model pinned by revision and SHA256 in `scripts/setup-model.ps1` and `src/Sitrep.Desktop/tessdata/README.md`. No runtime downloads.
- Apollyon L81 revision `e8d1ee9ceb07fe9ec4923b7b55e836f8b3bb3e05`: 132–684 m, linear interpolation, no extrapolation/terrain correction. All 84 upstream samples and limits independently compared with the local profile: exact match. See `src/Sitrep.Core/Data/PROVENANCE.md` for rights limitations.
- Always live, guarded (BUILD_SPEC revision 3; no permission or Gate E claim): no Enable/F10 barrier; capture is gated only by the foreground window's executable matching `GameProcessName` (default `wardogs`, case-insensitive substring so `*-Win64-Shipping` also matches), the optional `ForegroundTitleContains` fallback, or desktop test mode. Ctrl+MMB origin, Shift+MMB target, F9 clear; F8/F7 remain aliases; MMB state is polled only for the chord edge and a plain middle click never triggers capture. Because input is observed globally, F9 acts only while the attached game window or SITREP's own control window is in the foreground (there is no Clear button); F9 typed into an unrelated application is ignored. Foreground polling binds one HWND; loss hides the overlay and invalidates targets, closure clears origin. Focus changes reseed input; changing the game identity or test mode clears window attachment/origin.
- Map guard: `RoiBuilder.GetCenteredMapRegion` = centered square of `0.611 × client height` (880 px at 2560×1440, matching the reference screenshots' 840…1719 horizontal span). Clicks outside reject pre-capture as `OUTSIDE MAP AREA`, mirroring `MOVED` semantics (origin attempt clears origin; target attempt keeps origin). Skipped in desktop test mode and when client bounds are unavailable; this does not bypass the capture backend's required client/monitor bounds or the foreground/OCR gates. **Calibration is from screenshots only, not yet confirmed in game; windowed/letterboxed layouts may need the fraction adjusted.**
- Single control window (resizable, optional always-on-top) with auto-saved settings; `SettingsWindow` removed. Minimize hides only after successful native tray registration (`Shell_NotifyIcon`, no WinForms), otherwise the taskbar remains available. `TaskbarCreated` re-registers after Explorer restarts; failure reveals a hidden control window. Registration is idempotent, menu instances are closed on replacement/shutdown, and hooks/callbacks are detached before disposing the icon. Overlay owns lock/unlock window styles only; the control window persists `OverlayLeft/Top` on lock, so diagnostics never write the user's config. `OverlayLeft/Top` are nullable (`null` = default top-right placement) so negative coordinates from monitors left of / above the primary round-trip intact; the legacy `-1/-1` sentinel written by earlier builds migrates to `null` on load. `Unlock()` creates the overlay HWND (`WindowInteropHelper.EnsureHandle`) before changing styles, so unlocking before the overlay was ever shown no longer throws `Win32Exception` 1400. Process identity uses one retained safe handle with query-limited-information + synchronization rights (no memory access). Cache hits require a zero-time wait to confirm that exact process is still alive; PID reuse reopens/requeries instead of reusing a stale name. Denied lookups, and identities that are already dead (or whose liveness wait fails on the granted handle) immediately after opening, return no identity, are disposed, and retry after 1 s, not at input rate. Handles are disposed on replacement/shutdown. The separate status-only process scan stays at 2 s, skips enumeration while a matching process is foreground or test mode is enabled, and tolerates process-exit races. Case-insensitive basename substring and optional title-fallback semantics are unchanged.
- Dedicated input sampling thread (~10 ms high-bit polling, no hooks/raw input/synthesis) freezes modifiers, cursor, foreground HWND and event time before UI callbacks. A 64-gesture mailbox survives synchronous dispatcher capture; overflow invalidates work rather than building an unbounded backlog. Plain MMB and Ctrl+Shift+MMB are ignored; adding a modifier while MMB is held does not trigger. F8/F7 aliases coalesce with matching chords, simultaneous origin/target chooses origin, F9 wins its sample and clears the backlog. Disable/focus changes reseed and invalidate epochs; a sampled alt-tab-away/back cannot resurrect pending OCR. Snapshot on the UI dispatcher at the frozen anchor/ROI, then one 50 ms retry before OCR. Reject moved/missing cursor, >500 ms input/retry delay, clipping, own-window overlap, and disagreeing valid recipes/snapshots. Sampling still cannot guarantee detecting presses/focus changes entirely between samples. One OCR worker, at most one replaceable pending item; sampler and OCR worker drain before disposal.
- Debug diagnostics: off by default; at most **50 complete records (100 files)** under local app data, one PNG and matching JSON per snapshot. Timestamp + GUID names avoid collisions even for both snapshots of the same request. A per-directory named mutex serializes same-session application instances, bounded to a 2 s acquisition wait. Staged files publish only after both writes finish, failed writes roll back, and successful saves prune whole records and repair orphan/staging remnants. Locked/unwritable paths remain best effort (repair deferred until writable); no crash-atomic two-file filesystem guarantee or upload/telemetry.

- F9 dispatch preserves the complete sampled `InputGesture`. `ClearInputPolicy` requires a current epoch, unchanged nonzero foreground, and either the attached/allowed game HWND or the exact control HWND (not any SITREP-owned overlay). A focus transition, including away/back, still drops queued clears and invalidates epochs; a user who switches focus before F9 dispatch must press again. Retaining old clears could erase an origin established later. Foreground checks cannot detect an entire transition between samples.
- Parser isolation uses Unicode letter/number/mark/connector boundaries (including surrogate/format continuations), not ASCII-only boundaries. Adjacent signs/numeric continuations reject. Numeric-like malformed bodies (`xBAD1`, `x.77`, whitespace-separated numbers/signs) reject rather than disappearing ahead of a valid pair. Alphabetic words (`Yellow`, `YZ`, `xBAD`) and lone labels without numeric bodies are noise, never coordinates or duplicate numeric axes. The live OCR engine has no character whitelist: signs/letters must reach the parser rather than be erased into plausible numbers. Exact two-decimal precision, order/whitespace/decimal-comma support remain; no repairs or inferred bounds.
- Historical root `001`/`002`/`003` review/fix/close-out reports remain local history, not competing status documents. Their relevant overlay, tray, identity, input, packaging and verification notes are consolidated here; the old F9 close-out is superseded below. Root-only `/[0-9][0-9][0-9]-*.md` ignores numbered Markdown session reports without deleting them, hiding nested docs, or ignoring arbitrary Markdown. Unrelated greeting reports are untouched. Optional off-screen placement clamping from report 002 is not implemented; it was not a required defect fix.

## Audit resolution (010-Sitrep-audit.md)
| Findings | Root fix / regression evidence |
| --- | --- |
| P0-1 origin replacement | Invalidate usable origin immediately; target cannot supersede pending origin. `StateTests` and packaged orchestration regression. |
| P0-2 frozen capture / queue | Capture before OCR, preserve request ROI, dispose superseded frames. `LatestCaptureWorkerTests` and packaged busy-worker snapshot test. |
| P0-3 DPI / contamination / bounds | Native physical `GetWindowRect`, client/monitor intersection, exclude all visible SITREP windows. Negative-origin/clipping unit tests and native window/style checks at 96 DPI. Mixed-DPI physical trial still pending. |
| P0-4 foreground / closure | Continuous observer plus checks before both captures and publication. `ForegroundSessionTests`, delayed focus/closure completion checks. |
| P0-5 and P2 shutdown / failures | Owned draining worker, caught capture/OCR failures, UI-only state publication. Exception recovery/disposal and late-completion tests. |
| P1-1 through P1-4 | Await real WinExe exit with redirected streams/timeout; five mandatory negative images; public/private gates separated; locked publishing; fail-closed model checksum and immutable action/model pins. Real native/GUI nonzero-exit and missing/corrupt dependency probes. |
| P2 resources / robustness | Borrow input bitmap, dispose intermediates, LockBits grayscale/threshold pixel-parity check; validated/atomic config persistence with one startup error; corrupt table types fail closed and constructor private; F10 reseeding; normalized bearing display; explicit MOVED status; 50-file debug retention; bounded retry and consensus tests. |
| P3 hygiene / docs | Removed obsolete capture/gate helpers, redundant statuses/branches/settings, unused coverage package; x64 checked style APIs; warnings as errors; data copied to test output; RID rationale; correct Sitrep paths/overlay fields; packaged notices/model README and pinned table provenance. Rights inventory caveat below remains open. |

## Executed checks (Windows workstation, 2026-09-16)
- `dotnet --version`: 10.0.303. `dotnet build Sitrep.slnx -c Release`: 0 warnings, 0 errors.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1 -PrivateFixtures`: **exit 0 / VERIFY OK**. Locked restore/build/publish, **120 passed / 0 failed / 0 skipped**, packaged self-test/integration, failure probes, five negative images and both private positives. Executables run from unrelated temporary CWD; user config is not touched by diagnostics.
- Startup regression first reproduced six missing-actionable-message failures: `InvalidDataException` is not an `IOException`. Fixed contextual load errors and interactive catch path; full suite and packaged startup-handler checks now pass.
- Self-test accepts exactly x101.53 y107.77 using the live OCR path. Packaged integration checks preprocessing pixel parity/ownership, native overlay bounds/styles, invalid startup config, debug retention, frozen/busy snapshot queue, retry movement, OCR exceptions, and completion focus/closure guards.
- All five committed negative images reject (exit 2); none accepted. `conflict.png` is truncated and rejects on precision, so true recipe/snapshot conflicts also have deterministic regressions.
- Private `MapCords.png --crop 900,640,300,180`: x101.53 y107.77; `MapPing.png --crop 900,300,300,160`: x101.66 y110.10; both exit 0. Kept local, not committed/uploaded.
- Calculation fixture: 233.36237914454 m, 3.19344927328°, 755.63762085546 MIL. Independent table checks: 132→850, 229→760, 239→750, 300→690, 684→150; interior 400→583.33333333333, 500→461.42857142857; exact/outside limits tested. Display rounds MIL to 0.1; bearing rounds then normalizes below 360°.
- Branding & App Icon: `scripts/generate-icons.ps1` converts the master `SITREP.png` (local-only, gitignored, ~1.3 MB) to multi-resolution `Assets/app.ico` (16 to 256 px) and `Assets/logo-64.png`; only these two referenced assets are committed and embedded (unreferenced 512/32 px PNGs were dropped on 2026-09-18). Integrated into `Sitrep.Desktop.csproj` (`ApplicationIcon` PE embedding, WPF resources), `MainWindow.xaml` (title-bar icon and header logo badge), and `MainWindow.xaml.cs` (tray icon loaded from the pack resource at the window's DPI; the `Icon` object is held for the tray entry's lifetime and disposed on close).
- Local artifact: `out/Sitrep-win-x64/` and `out/Sitrep-win-x64.zip`, with notices/provenance/model. Visual C++ x64 runtime is a separate prerequisite. No release or installer produced.

## Executed checks (Windows workstation, 2026-09-17 — single-window / always-live / tray / Ctrl+MMB revision)
- `dotnet build Sitrep.slnx -c Release`: 0 warnings, 0 errors. `dotnet test`: **133 passed / 0 failed / 0 skipped** (new: 1440p/1080p/ultrawide map-region geometry, half-open map-guard bounds, degenerate client, `LiveEnabled` default, `OUTSIDE MAP AREA` origin/target semantics).
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1`: **exit 0 / VERIFY OK** (public fixtures; private screenshots not present on this checkout). Locked restore/build/publish, packaged self-test, failure probes, five negative images.
- Packaged `--integration-test` (from `%TEMP%`): INTEGRATION OK, now also covering the overlay unlock → hit-testable-but-non-activating → lock round-trip (`WS_EX_TRANSPARENT` cleared then restored), the map guard (gutter click at 2560×1440 rejects with zero captures/OCR; desktop test mode proceeds to capture), unlock on a never-shown overlay (HWND created, hit-testable, layered + non-activating, then re-locked), and the negative overlay position round-trip (`-1920,-50` applied → `Lock()` records it → JSON save/load → fresh window restores it → `ResetPosition` clears to `null`; legacy `-1/-1` loads as unset while `-1/-2` is kept). Both new checks were confirmed to fail against the pre-fix code (`Win32Exception 1400` and "Negative saved overlay position was not applied").
- GUI smoke: both the Release build and the published `out/Sitrep-win-x64/Sitrep.exe`, launched from `%TEMP%`, show the `SITREP` window, stay responsive for 5 s, exit with code 0 on close, leave no `Sitrep` process, and do not modify `%LocalAppData%/Sitrep/config.json` at startup.
- Not exercised here (needs a human at the desktop): tray icon visibility/menu dismissal, overlay drag/double-click lock, Ctrl/Shift+MMB delivery with the game in the foreground, the `wardogs` executable name, and the map-square calibration.

## Executed checks (Windows workstation, 2026-09-18 — review close-out / cleanup)
- Historical close-out added a dispatcher-time F9 foreground gate (`MainWindow.OnClear` re-observed the foreground first). The focused-hardening entry below supersedes this incomplete fix: the sampled gesture was dropped and any process-owned window could authorize clearing. Removed dead code left by the input/tray rework: `Sitrep.Core.InputEdges` (superseded by `InputSampling`; its phantom-edge regression was ported to `InputSampling` in `AuditRegressionTests`), the unused `DangerButtonStyle`, and the unreferenced embedded `logo.png`/`logo-32.png` (~320 KB less in the assembly). Deleted the stale root `PLAN.md` (BUILD_SPEC §1: no duplicate planning documents; this file is the record).
- `dotnet build Sitrep.slnx -c Release`: 0 warnings, 0 errors. `dotnet test`: **147 passed / 0 failed / 0 skipped** (new since 09-17: `DesktopHardeningTests` for `TrayRegistration`, `ProcessIdentityCache`, `InputSampling` chords/aliases/barrier/backlog and frozen event time).
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1`: **exit 0 / VERIFY OK** (public fixtures). Packaged `--integration-test`: INTEGRATION OK including native process identity/matching and blocked-dispatcher input chords/disposal; failure-path probes passed; five negative images rejected.
- GUI smoke of the published `out/Sitrep-win-x64/Sitrep.exe` from `%TEMP%`: `SITREP` main window created within 5 s (confirms the trimmed resource set still resolves), exit code 0 on close, no leftover `Sitrep` process, `%LocalAppData%/Sitrep/config.json` hash unchanged.
- Not exercised here: the F9 foreground gate at runtime (two-line guard in a WPF window; no automated MainWindow harness exists) — added to the Gate E checklist below.

## Executed checks (Windows workstation via WSL, 2026-09-18 — focused hardening executor)
- New executions, not a restatement of reports 001–003: Windows PowerShell 7 / pinned SDK 10.0.303; `dotnet build Sitrep.slnx -c Release` **0 warnings / 0 errors**; `dotnet test tests/Sitrep.Tests -c Release` **198 passed / 0 failed / 0 skipped** (+51 from 147: 38 parser-boundary/positive/consensus/state cases and 13 clear-policy/epoch/state cases).
- `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1 -PrivateFixtures`: **exit 0 / VERIFY OK (private fixtures: True)**. Cached model SHA verified, locked restore/build/test/publish, packaged self-test, integration at 96 DPI, native/GUI/dependency failure probes, all five negative fixtures rejected (exit 2), both available private positives exactly accepted (exit 0). Logs/artifacts remain local in ignored `out/autonomous-hardening-executor.log`, `out/Sitrep-win-x64/`, and `out/Sitrep-win-x64.zip`; checks launch the executable from an unrelated temporary CWD. No commit/push/release.
- Pre-fix parser reproduction: initial 33 added cases produced **22 failures / 11 passes** against the original parser (initially classified as 20 false accepts plus 2 consensus/state failures), including `_x101.53 y107.77`, `éx101.53 y107.77`, suffix underscore/combining marks, `xBAD` alongside a valid pair and adjacent signed numeric components. The controller correction below supersedes the classification of the two purely alphabetic `xBAD`/`yBAD` noise cases; their initial expected rejection was overbroad. Existing signed values, glued ASCII letters and extra decimal components already rejected; retained regression coverage. Separate grid numbers are permitted noise, not evidence of a coordinate continuation.
- Closed-loop correction: the first full private gate passed all public checks but rejected `MapPing.png` because the first candidate-isolation change incorrectly treated a lone trailing `y` as a malformed extra coordinate. Two positive raw-text regressions reproduced that failure; narrowed body-candidate detection, kept every negative assertion and reran the full gate successfully. `MapCords.png --crop 900,640,300,180` → (101.53,107.77); `MapPing.png --crop 900,300,300,160` → (101.66,110.10). These local images are present in this session (earlier reports recorded them absent); neither was changed or uploaded. First-attempt log: `out/autonomous-hardening-private-first-attempt.log`.
- `CheckClearDispatchAsync` exercises the real independent `InputMonitor` sampling thread and WPF dispatcher timer with the existing injected read boundary. It verifies frozen foreground/modifiers/cursor/time/epoch after key release and cursor movement, exact-control/attached-game authorization versus unrelated/overlay and later-focus cases, focus-round-trip dropping queued clears, and disposal. A dispatcher-time resampling mutation failed with **“Clear dispatch lost its frozen gesture”**, exit 1 (`out/f9-dispatch-mutation.log`); the correct payload was restored and the packaged gate passed. Pure tests also prove stale clears cannot erase later origins and authorized clears reject old OCR completions. No native input was synthesized.
- Limits: this tests dispatch plus the shared policy, not human F9 delivery through `MainWindow` or the game. No new GUI/tray/drag smoke, mixed-DPI trial, latency/CPU/memory measurement or rights clearance is claimed; historical GUI evidence above remains historical. Gate E remains pending.

## Executed checks (2026-09-18 — controller live-OCR hardening)
- After an independent reviewer returned no findings at 198 tests, the controller reproduced a pre-existing live-path false accept: a synthetic image reading `x-101.53 y107.77` became positive (101.53,107.77), exit 0. `tessedit_char_whitelist` erased the sign in both recipes. Removed the whitelist rather than repairing text or adding an engine/recipe. The identical packaged probe now exits 2 with `UNSUPPORTED_SIGN`; before/after logs are in ignored `out/signed-coordinate-probe-{before,after}.log`.
- Added `CheckOcrTokenEvidence` to the canonical packaged integration path: real native engine, both preprocessing recipes, four signed/letter-glued negatives plus one exact positive control. It failed against the whitelist implementation, then passed without it. These synthetic cases are bounded regression evidence, not general OCR accuracy proof.
- Removing the whitelist exposed real `MapPing` OCR noise (`YZ`) that the new candidate regex incorrectly treated as another coordinate. Corrected that rule to require numeric-like evidence; four positive unit cases failed before this correction. **Test expectation correction:** the executor's two `xBAD`/`yBAD`-beside-valid-pair negatives conflated ordinary alphabetic words with numeric coordinate labels. Reclassified them as positive noise controls, retained missing-axis rejection when they are the only label, and added `xBAD1`/`yBAD1` malformed-numeric negatives. No precision, sign, boundary, duplicate/conflict, or private-image expected-coordinate assertion was relaxed.
- Controller's final `scripts/verify.ps1 -PrivateFixtures`: **exit 0 / VERIFY OK**, **204 passed / 0 failed / 0 skipped**, Release **0 warnings / 0 errors**, locked restore/publish, packaged OCR/F9/other integration checks, failure probes, five committed negatives rejected, both private positives exact. Log: `out/autonomous-hardening-controller-final.log`; local folder/ZIP rebuilt. The initial no-whitelist private run failed on the alphabetic-noise issue above; only the final corrected run is the current gate evidence.
- Architecture, implementation and first adversarial review were sequential specialist runs; the first review did not detect the whitelist defect, so its clean verdict is not evidence for this subsequent patch. **Final independent review blocked:** two follow-up Opus launches failed during Duo CLI initialization (GitLab version fetch/authentication unavailable), with no result; child/launcher/runner processes were confirmed exited. Final code is controller-tested, not independently re-reviewed. Run IDs/logs are retained in the controller report. Gate E, mixed-DPI/manual UX, measured performance and permission/rights remain unverified.

## Blockers / pending
- PR #3: initial `f97c58c` Windows push/PR verification and CodeQL passed; SonarCloud reported 14 annotations, resolved in `ed67ffc`. Review comments from CodeRabbit addressed: checkout credential persistence disabled in CI; `LatestCaptureWorker` protected against failure callback / disposal exceptions with dedicated unit test; `RecognitionConsensus` immediately rejects parser `CONFLICTING_PAIR` as `CONFLICTING_RECIPES` with regression tests; `AssistantService` classifies window closure and foreground loss before capture failure / movement in both capture paths; SDK runtime notices explicitly validated during MSBuild packaging and publish script. That historical revision had 120 tests; current results are recorded above.
- **Not game-validated:** publisher approval, the `wardogs` executable name, real click-through and Ctrl/Shift+MMB delivery (and whether the game binds those chords itself) with trigger loss under load, map-square calibration in the user's display mode, cursor-label alignment/edges/zoom/UI scale, mixed-DPI displays, rapid targets/F9/alt-tab/exit in game, tray restore/menu, overlay drag-lock, 30 captures, p95 latency, idle CPU and repeated-OCR memory measurements. Native styles at 96 DPI and fake-boundary tests do not prove these.
- **Redistribution review remains open:** upstream excludes third-party material from its MIT grant; table authorship/rights are not independently established. Native codec/subdependency inventory is not fully supplied by the wrapper package. Top-level Tesseract, Leptonica (BSD, not Apache), InteropDotNet, model, data and SDK runtime notices are included, but this is not complete legal clearance for public binary distribution.
- Completion label: **Implemented, offline trial gates passed**. No blanket production/live-use all-clear; remaining real-input and rights checks must not be inferred from synthetic tests.

## Next step
Confirm publisher permission and redistribution scope, then perform the BUILD_SPEC Gate E checklist on the actual supported display/game configuration: verify the header pill reaches `IN GAME` (else copy the "Last seen app" name into *Game executable name*), Ctrl+MMB / Shift+MMB against the reference labels, one gutter click for `OUTSIDE MAP AREA`, F9 in game (clears) and F9 in another application (ignored), alt-tab, overlay unlock → drag → double-click, minimize → tray → restore → Exit. Keep the trial local until permission is resolved.
